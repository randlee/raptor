# Markdown validation

Invoke `markdown-validate` through the Agent Runner with repository root, input, profile, and explicit reference mode. It delegates to `scripts/validate.py markdown` and never mutates.

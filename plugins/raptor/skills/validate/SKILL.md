---
name: validate
description: Route validation of Markdown, JSON indexes, and SQLite stores.
metadata:
  version: 1.0.0
---

# Raptor Validate

Read only the selected reference: `references/markdown.md`,
`references/json.md`, or `references/sqlite.md`. Run its command exactly and
report its diagnostics without changing source input.

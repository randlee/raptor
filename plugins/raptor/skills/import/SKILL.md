---
name: import
description: Route Markdown-to-JSON and JSON-to-SQLite imports.
metadata:
  version: 1.0.0
---

# Raptor Import

Select the reference matching the requested source and target:

- Markdown → JSON: read `references/markdown-json.md`.
- JSON → SQLite: read `references/json-sqlite.md`.

Run the selected procedure exactly. Each procedure uses the checked-in scripts
and leaves source Markdown unchanged.

# Migration round trip

Invoke `migration-round-trip` through the shared Agent Runner with explicit Markdown input, intermediate JSON, SQLite database, exported JSON, rendered Markdown output, profile/template-set identity, and validate/apply intent. The agent composes registered operations and returns path-level semantic differences without raw tool traces.

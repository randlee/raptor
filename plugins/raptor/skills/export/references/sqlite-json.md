# SQLite → JSON

Invoke `sqlite-json-export` through the Agent Runner with repository root, database, composite document key, output, and validate/apply intent. The agent delegates to `scripts/export_sqlite.py`; validation never writes.
